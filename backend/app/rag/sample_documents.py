SAMPLE_DOCUMENTS: dict[str, dict[str, str]] = {
    "redis-basics": {
        "title": "Redis 缓存使用规范",
        "filename": "Redis缓存使用规范.md",
        "content": """
# Redis 缓存使用规范

## 文档摘要

本文档说明 Redis 在业务系统中的缓存使用规范，包括缓存创建、键命名、过期时间、读取流程、更新策略和常见风险。

## 适用范围

适用于需要使用 Redis 做缓存的后端服务，例如用户信息缓存、商品详情缓存、权限数据缓存、配置缓存和热点数据缓存。

## 术语说明

- 缓存命中：请求的 key 在 Redis 中存在，直接返回缓存数据。
- 缓存未命中：请求的 key 不存在，需要查询数据库或其他数据源。
- TTL：key 的剩余过期时间。
- 热点数据：访问频率很高的数据，例如秒杀商品、首页配置、热门榜单。

## 键命名规范

Redis key 应使用业务域前缀，格式建议为：

```text
业务域:对象类型:对象ID
```

示例：

```text
user:profile:10001
product:detail:9527
auth:permission:admin
```

## 创建缓存流程

标准读取流程：

1. 根据业务参数生成 Redis key。
2. 查询 Redis。
3. 如果命中，反序列化后返回。
4. 如果未命中，查询数据库。
5. 将数据库结果写入 Redis，并设置过期时间。
6. 返回查询结果。

示例命令：

```text
SET product:detail:9527 "{...json...}" EX 3600
GET product:detail:9527
```

## 过期时间建议

- 用户资料：10 到 30 分钟。
- 商品详情：5 到 60 分钟。
- 系统配置：1 到 24 小时。
- 热点数据：可使用逻辑过期或定时刷新。

## 更新策略

推荐策略是先更新数据库，再删除缓存。删除缓存后，下一次读取会重新加载数据库并写入缓存。

## 注意事项

- 不允许所有 key 使用相同过期时间，避免同一时间大量缓存失效。
- 大 value 需要谨慎缓存，避免占用过多内存和网络带宽。
- 缓存空值时必须设置较短过期时间，避免缓存穿透。
- 高并发热点 key 需要考虑互斥锁或逻辑过期。

## 常见问题

### Redis 如何创建缓存？

使用 SET 命令写入 key 和 value，并用 EX 设置过期时间。例如：

```text
SET user:profile:10001 "{...json...}" EX 1800
```

### 缓存没有命中怎么办？

缓存未命中时查询数据库，然后把查询结果写入 Redis，再返回给调用方。
""",
    },
    "redis-lock": {
        "title": "Redis 分布式锁设计规范",
        "filename": "Redis分布式锁设计规范.md",
        "content": """
# Redis 分布式锁设计规范

## 文档摘要

本文档说明 Redis 分布式锁的使用场景、加锁方式、释放方式、超时时间设置和常见风险。

## 适用范围

适用于多个服务实例同时竞争同一资源的场景，例如订单防重复提交、库存扣减、定时任务抢占和幂等控制。

## 使用场景

- 防止同一订单被重复处理。
- 防止库存并发扣减导致超卖。
- 保证同一用户的关键操作串行执行。
- 多实例定时任务中只允许一个实例执行。

## 加锁规范

推荐使用 SET 命令同时指定 NX 和 EX：

```text
SET lock:order:1001 random-value NX EX 10
```

参数含义：

- lock:order:1001：锁 key。
- random-value：锁持有者唯一标识。
- NX：只有 key 不存在时才写入。
- EX 10：设置 10 秒过期时间。

## 释放锁规范

释放锁时必须先判断锁 value 是否属于当前请求，不能直接 DEL。生产环境建议使用 Lua 脚本保证判断和删除的原子性。

示例逻辑：

```text
if redis.get(lockKey) == requestId:
    redis.del(lockKey)
```

## 超时时间设置

锁过期时间应略大于业务正常执行时间。如果业务可能超过锁过期时间，需要使用续期机制。

## 风险与处理

- 锁过期太短：业务未完成锁已释放，可能导致并发执行。
- 锁过期太长：服务异常后其他请求等待时间过长。
- 误删锁：释放锁时没有校验 value，可能删除其他请求持有的锁。
- 单点风险：极高可靠性场景可以考虑 Redisson 或多节点方案。

## 常见问题

### Redis 如何创建分布式锁？

使用 SET key value NX EX seconds。例如：

```text
SET lock:product:9527 request-uuid NX EX 10
```

### 为什么 value 要使用随机值？

随机值用于标识锁持有者。释放锁时只有 value 匹配才允许删除，避免误删别人的锁。

""",
    },
    "agentpilot-intro": {
        "title": "AgentPilot 功能说明文档",
        "filename": "AgentPilot功能说明文档.md",
        "content": """
# AgentPilot 功能说明文档

## 文档摘要

AgentPilot 是一个 AI 智能体工作台，支持创建智能体、导入知识库、配置模型、运行任务，并展示回答、引用来源和执行过程。

## 目标用户

- 想搭建知识库问答系统的开发者。
- 想学习 LLM、RAG、向量检索和智能体工程的学生。
- 需要面试展示项目的后端或全栈开发者。

## 核心流程

1. 创建知识库。
2. 导入文档。
3. 使用检索测试验证文档是否可被命中。
4. 创建智能体。
5. 将智能体绑定到知识库。
6. 配置模型接口和 Token。
7. 在运行任务页面输入问题。
8. 查看智能体回答、执行过程和引用来源。

## 核心能力

- 大模型网关：支持 OpenAI-compatible API 和 Ollama。
- 知识库 RAG：支持文档导入、切块、检索和引用来源。
- 切面能力：支持 trace_id、耗时统计和执行过程记录。
- 向量能力：支持 embedding 和相似度检索。

## 第一版验收标准

第一版需要跑通：

```text
创建知识库 -> 导入文档 -> 检索测试 -> 创建智能体 -> 执行任务 -> 查看回答和引用
```

## 注意事项

- 当前第一版仍使用内存存储，后端重启后用户手动创建的数据会丢失。
- Token 不写入前端代码，用户需要在模型配置页面输入自己的 Token。
- 没有配置 Token 时，系统会返回开发模式回答。

## 常见问题

### 为什么运行任务时没有智能体？

需要先进入“智能体”页面创建智能体。后续版本会接入数据库持久化，避免重启后丢失。

### 为什么回答不是大模型真实结果？

需要在“模型配置”页面填写可用的大模型接口地址、API Token 和默认模型。
""",
    },
}
